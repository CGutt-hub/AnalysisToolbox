# EmotiView AnalysisToolbox — Python dependencies
# Install the packages relevant to the workflows you intend to run:
#   pip install -r requirements.txt
# All packages are optional — only install what your chosen modules need.

numpy
scipy
polars
pyarrow
mne
mne-connectivity
pyxdf
statsmodels
pingouin
